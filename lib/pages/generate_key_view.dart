import 'package:flutter/material.dart';

class GenerateKeyView extends StatelessWidget {
const GenerateKeyView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(
        title: const Text('Generate Key'),
      ),
      body: Container(),
    );
  }
}